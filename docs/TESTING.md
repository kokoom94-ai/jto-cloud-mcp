# 시험 기록

2026-09-17 공개 모드의 개발 시험 및 실제 Render 서버 생성·다운로드 검증을 완료했습니다. 각 AI 앱의 실제 대화창 시험이나 한컴 한글 실조판까지 완료했다는 뜻은 아닙니다.

## 통과

- 원본 세 파일의 SHA-256 및 양식 선택 규칙.
- 사업계획·결과보고 HWPX 생성, 필수 ZIP/XML 구성·표 병합 구조 검사.
- HWPX 원본 header.xml 및 BinData 바이트 보존.
- 1PAGE HWP 생성, OLE 스트림 재구성, 레코드 재파싱, DocInfo·FileHeader 보존.
- 예산 합계 불일치, 결과보고의 추정 집행액, 미등록 출처 ID, 1PAGE 과다 분량 거절.
- OAuth 등록 → PKCE → 동의 → 토큰 발급 → MCP 초기화 → 세 양식 생성 → 서명 URL 다운로드 통합시험.
- 선택적 OAuth 모드의 인증 없는 MCP 접근, 재사용한 인증 코드, 회전 전 갱신 토큰, 변조·만료 다운로드 링크, 다른 연결의 문서 조회 거절.
- 별도 문서 렌더러에서 예시 1PAGE를 1쪽으로 확인. 사업계획·결과보고 예시는 3쪽으로 렌더됨.

자동 시험: `pytest -q` — 9개 통과. 의존 도구의 deprecation warning 2건은 기능 실패가 아닙니다.

## 남은 검증

- ChatGPT·Claude·Gemini 앱에서 각각 도구 등록과 실제 대화 중 파일 받기. ChatGPT 확인 시 브라우저는 로그아웃 상태였으며 계정별 연결은 실행하지 않았습니다.
- 한컴 한글에서 최종 조판·인쇄·1쪽 여부. 외부 렌더러는 한컴 엔진과 동일하지 않습니다.
- 최대 길이·다양한 실제 업무 내용의 레이아웃 검증.
- 조직용 SSO/직원별 권한, 다중 인스턴스, 장기 부하 시험은 현재 범위에 포함되지 않습니다.

공개 모드 시험: 무인증 MCP 초기화·양식 조회·세 양식 생성·다운로드, 잘못된 파일 서명 거절, 누락/잘못된 재조회 키 거절, 올바른 키 재조회.

## 실제 온라인 검증 기록

- 시각: 2026-09-17 10:50 KST.
- 검증한 배포 코드: `eb06bd496b3b404085ab59c4f1d05e9a640757b3`.
- 서버: https://jto-cloud-mcp.onrender.com/mcp
- Authorization 헤더·로그인·암호 없이 MCP initialize, tools/list 및 jto_template_info 실제 호출 성공.
- 세 양식에 대해 jto_start_document와 jto_generate_document 호출 성공.
- 양식당 8개, 총 24개 파일을 외부 HTTPS로 내려받아 Content-Disposition, 크기, ZIP/OLE 구성을 확인.
- 변조·서명 누락 링크는 404. 재조회 키 누락·오류는 거절, 올바른 키는 성공.
- 내려받은 실제 파일의 별도 렌더 결과: 사업계획 3쪽, 결과보고 3쪽, 1PAGE 1쪽. 검증용 짧은 예시의 표지·본문·예산표에서 눈에 띄는 잘림/겹침을 발견하지 않음. 모든 입력과 한컴 한글의 조판을 보증하지 않음.
- [GitHub Linux 자동 시험 9개 통과](https://github.com/kokoom94-ai/jto-cloud-mcp/actions/runs/35171978234).
- 현재 무료 인스턴스: 유휴 지연 및 재시작 시 생성 파일 손실 가능. 유료 지속 저장 구성은 준비만 했고 적용하지 않음.
